.contact.page
    .contact__layer.page__layer
        .center
            +breadcrumbs(['Головна', 'Контакти'])
            .f-container.contact__container
                .contact__main.page__main
                    .contact__main-layer
                        iframe#contact__map.contact__map(width='100%' height='100%' src='' frameborder='0' scrolling='no' marginheight='0' marginwidth='0') 
                aside.contact__sidebar.contacts.sidebar_size_l
                    .contact__sidebar-layer
                        +contacts('contact__sidebar')

